server:
  port: 51701
spring:
  redis:
    host: localhost
    port: 6379
  application:
    name: water-schedule
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
