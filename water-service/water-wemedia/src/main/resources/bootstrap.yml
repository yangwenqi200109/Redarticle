server:
  port: 51804
spring:
  application:
    name: water-wemedia
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml

#  redis:
#    host: localhost
#    port: 6379
