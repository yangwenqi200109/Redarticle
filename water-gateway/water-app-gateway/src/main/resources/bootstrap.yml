server:
  port: 51601
spring:
  application:
    name: water-app-gateway.yml
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml

